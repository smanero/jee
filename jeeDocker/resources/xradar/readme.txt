If you are using a proxy the DTD URL's won't be accessible.
You can add the following properties to your ANT_OPTS environment variable:

-DproxySet=true -DproxyHost=<your proxy host> -DproxyPort=<your proxy host
port>